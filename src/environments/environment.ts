// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: 'AIzaSyA1tbvWdB3GhCTnmekJiipFFYieDxuh8Pc',
    authDomain: 'escapethecube-fb.firebaseapp.com',
    databaseURL: 'https://escapethecube-fb.firebaseio.com',
    projectId: 'escapethecube-fb',
    storageBucket: 'escapethecube-fb.appspot.com',
    messagingSenderId: '738506709251',
    appId: '1:738506709251:web:0cc986fb525fe00aa4f136',
  },
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
